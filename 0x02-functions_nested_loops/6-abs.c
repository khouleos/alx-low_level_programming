#include "main.h"
/**
 * _abs - Compute the absolute value of an intger
 * @n: the number to be checked
 * Return: Absolute value
 */
int _abs(int n)
{

	if (n < 0)
	{
	int abs = n * -1;

	return (abs);
	}
	else
	return (n);
	else
}
